version: '2.1'
services:
  mongodb_container:
    image: mongo:latest
    environment:
      - MONGODB_DATABASE="challenge"
    ports:
      - 54322:54322
    volumes:
      - ./data:/data/db

networks:
  internal:
